# Formas tradicionais de declarar variaveis

```ts
let idade = 25;
const nome = "Malu";
var cidade = "Campinas";
```

Na prática moderna de TypeScript:

`const` → padrão quando o valor não será reatribuído.

`let` → quando você precisa reatribuir.

`var` → evite em código moderno.

